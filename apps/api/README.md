# API application

The API will expose source health, normalized opportunities, recommendation packages, authenticated decisions, and audit history.

Implementation begins after Brief `0002` and its exam pass Gates 1 and 2. The first API boundary must use the domain contracts in `packages/domain/` and enforce organization scope plus human-only decision transitions.

